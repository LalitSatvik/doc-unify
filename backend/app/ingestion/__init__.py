"""Per-format document extractors, normalized to a common `ContentBlock`
interface (text, table, image-region; source doc/page + bbox).

v1 targets: pdf.py, image.py, docx.py, pptx.py. `video.py` is left as a
documented stub against the same interface — not implemented in this
phase (see docs/plan.md).

Landing in Phase 2 of docs/plan.md; empty for now (Phase 1 scaffolding).
"""
