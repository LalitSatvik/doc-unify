"""Candidate-field extraction, cross-corpus clustering, and LLM cluster
review that produces a proposed unified schema + conflict flags, plus the
schema registry CRUD and approval workflow.

Landing in Phase 4-5 of docs/plan.md; empty for now (Phase 1 scaffolding).
"""
