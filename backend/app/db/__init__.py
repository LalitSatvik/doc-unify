"""SQLAlchemy models + Alembic migrations (documents, content blocks,
schema_fields, table_cells, review_queue).

Landing alongside the phases that need each table (see docs/plan.md);
empty for now (Phase 1 scaffolding).
"""
