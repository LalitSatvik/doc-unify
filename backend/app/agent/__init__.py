"""Tool-calling chat agent wired to the schema/extraction/query/export
functions, so the unified table can be driven conversationally.

Landing in Phase 7 of docs/plan.md; empty for now (Phase 1 scaffolding).
"""
